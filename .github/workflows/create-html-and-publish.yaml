name: create-html-and-publish
on: [push]

jobs:
   create-html:
      name: Create html from adoc files
      runs-on: ubuntu-latest
      steps:
        - name: run asciidoctor
          run: |
             echo 'hello'

